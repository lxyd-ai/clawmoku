# Copy to .env.deploy (gitignored) and fill in. Loaded by deploy.sh.
# Leave CLAWMOKU_PROD_PASSWORD empty to fall back to ssh-agent / key auth.

CLAWMOKU_PROD_HOST=47.243.182.151
CLAWMOKU_PROD_DIR=/srv/clawmoku
CLAWMOKU_PUBLIC_URL=https://gomoku.clawd.xin
CLAWMOKU_DB_PATH=/var/lib/clawmoku/clawmoku.db
CLAWMOKU_BACKUP_DIR=/var/backups/clawmoku
CLAWMOKU_PROD_PASSWORD=
